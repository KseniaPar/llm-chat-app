spring:
  main:
    web-application-type: none
    banner-mode: off
    log-startup-info: false
  ai:
    mcp:
      server:
        name: mcp-git
        stdio: true

git:
  repo:
    root: ${GIT_REPO_ROOT:}

logging:
  level:
    root: WARN
