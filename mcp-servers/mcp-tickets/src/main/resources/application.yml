spring:
  main:
    web-application-type: none
    banner-mode: off
    log-startup-info: false
  ai:
    mcp:
      server:
        name: mcp-tickets
        stdio: true

tickets:
  dir: ${TICKETS_DIR:data/tickets}

logging:
  level:
    root: WARN
